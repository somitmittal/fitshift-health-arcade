# FitShift Health Arcade

FitShift is a single-file 2D health awareness arcade game built with Tailwind CSS and vanilla JavaScript.

## Run locally

Open `outputs/fitshift.html` in a browser.

## Deploy on Netlify

The included `netlify.toml` publishes the `outputs` folder as a static site. Connect this repository to Netlify and deploy with no build command.
